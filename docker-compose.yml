version: "3.8"

services:
  mongo:
    image: mongo:4.2.5
    ports:
      - "27017:27017"

  myapp:
    build: .
    ports:
      - "8080:8080"
    environment:
      MONGO_URI: mongodb://mongo:27017
      SECRET_KEY: sheffg
    depends_on:
      - mongo
